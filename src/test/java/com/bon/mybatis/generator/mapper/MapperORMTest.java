package com.bon.mybatis.generator.mapper;

import java.io.IOException;
import org.junit.Test;


public class MapperORMTest {
	
	@Test
	public void test_mapper() throws IOException {
	/*	Reader reader = Resources.getResourceAsReader("mybatis.cfg.xml");
		SqlSessionFactory sessionFactory = new SqlSessionFactoryBuilder().build(reader);
		PayOrderMapper mapper = sessionFactory.openSession().getMapper(PayOrderMapper.class);
		List<PayOrder> list =  mapper.selectByExample(new PayOrderExample());
		System.out.println(list);*/
	}
	
}
